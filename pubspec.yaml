name: flutter_chart
description: Flutter Canvas draws Bezier curves and histograms, embeds animations, and the entire drawing process is clearly visible. The gesture touch function for graphs, line graphs, and histograms is now supported. It is also worth noting that the histograms are now supported. Drawing of rounded corners. a very interesting gadget
version: 0.1.5
author: xt.sun<xingtao.sun@gmail.com>
homepage: https://github.com/good-good-study

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

